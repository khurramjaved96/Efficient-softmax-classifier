# effecient_softmax_classifier
An efficient implementation of neural network with softmax classifier. Can be used as actors in actor critic algorithm. We use it for MNIST classification in this repo 
